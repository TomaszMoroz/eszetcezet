{
  "name": "eszetcezet",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
  "dev": "vite",
  "dev:pa": "vite --open /admin",
  "build": "vite build",
  "prebuild": "node ./scripts/prebuild.js",
    "preview": "vite preview",
    "generate-manifest": "node ./scripts/generate-manifest.js",
    "generate-posters": "node ./scripts/generate-posters.js",
    "generate-posters:update-manifest": "node ./scripts/generate-posters.js --update-manifest",
    "add-font": "node ./scripts/add-font.js"
  },
  "dependencies": {
    "vue": "^3.5.18",
    "vue-router": "^4.5.1",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.2"
  }
}
